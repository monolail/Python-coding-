<이분 검색>
sys.stdin=open("input.txt", "r")

N,M = map(int, input().split())

arr = list(map(int, input().split()))

n = len(arr)
for i in range(n):
    for j in range(0, n-i-1):
        if arr[j] > arr[j+1]:
            arr[j], arr[j+1] = arr[j+1], arr[j]

def Binary_search(arr) :
    first = 0
    end = N-1

    while first <= end :
        mid = (first + end) // 2
        if arr[mid] == M :
            return mid
        
        elif arr[mid] > M:
                end = mid - 1
            

        else :
            first = mid + 1

    return -1

print(Binary_search(arr)+1)


# ver2
def binary_search(array, target, start, end):
    while start <= end :
        mid = (start + end) // 2

        if array[mid] == target :
            return mid
    
        elif array[mid] > target :
            end = mid - 1
        
        else :
            start = mid + 1

    return None



N,M = map(int,input().split())

gap = list(map(int,input().split()))

new_gap = sorted(gap)

result = binary_search(new_gap, M, 0, N - 1)

print(result + 1)

*버블 정렬*
n = len(arr)
for i in range(n):
    for j in range(0, n-i-1):
        if arr[j] > arr[j+1]:
            arr[j], arr[j+1] = arr[j+1], arr[j]


# 예제 정답
N,M = map(int, input().split())

arr = list(map(int,input().split()))

arr = sorted(arr)

rt = N-1
lt = 0

while (lt <= rt) :
    mid = (lt+rt) // 2

    if arr[mid] == M :
        break

    elif arr[mid] > M :
        rt = mid - 1
    else :
        lt = mid + 1

print(mid+1)

<랜선 자르기>
import sys
sys.stdin=open("input.txt", "r")

def Count(len) :
    cnt = 0
    for x in arr :
        cnt += (x//len)
    return cnt

N,M = map(int, input().split())
largest = 0
arr = []

res = 0
for i in range(N) :
    a = int(input())
    arr.append(a)
    largest = max(largest, a)


lt = 1
rt = largest

while lt <= rt :
    mid = (lt + rt) // 2
    if Count(mid) >= M :
        res = mid
        lt = mid + 1
    else :
        rt = mid -1

print(res)

<뮤직 비디오>
import sys
sys.stdin=open("input.txt", "r")

def Count(capacity) :
    cnt = 1
    hap =0
    for x in L :
        if hap + x > capacity :
            cnt +=1
            hap = x
        else :
            hap += x
    return cnt

N,M= map(int,input().split())

L = list(map(int,input().split()))
largest = max(L)
lt = 1
rt = sum(L)
res=0

while (lt <= rt) :
    mid = (lt + rt) // 2
    if mid >= largest and Count(mid) <= M :
        rt = mid -1
        res = mid

    else :
        lt = mid +1

print(res)

<마구간 정하기>
import sys
sys.stdin=open("input.txt", "r")

def Count(len) :
    cnt = 1
    ep = Line[0]
    for i in range(1,N) :
        if Line[i] - ep>= len :
            cnt +=1
            ep = Line[i]

    return cnt

N, C = map(int,input().split())

Line = []

for i in range(N) :
    gap = int(input())
    Line.append(gap)

Line.sort()
lt = 1
rt = Line[N-1]

while lt <= rt :
    mid = (lt + rt) //2

    if Count(mid) >= C :
        res = mid
        lt = mid + 1
    else :
        rt = mid -1

print(res)

<회의실 배정>

import sys
sys.stdin=open("input.txt", "r")

N = int(input())

meeting = []

for i in range(N) :
    s,e = map(int,input().split())
    meeting.append((s,e))

# 끝나는 시간대로 정렬
meeting.sort(key = lambda  x :(x[1],x[0]))

et = 0
cnt = 0

for s,e in meeting :
    if s >= et :
        cnt += 1
        et = e
print(cnt)

<씨름 선수>
import sys
sys.stdin=open("input.txt", "r")

N = int(input())

Profile = []

for i in range(N) :
    H,W = map(int,input().split())
    Profile.append((H,W))

# 키는 이제 고려X
# 가장 키 큰 사람을 제외함으로써, 몸무게로만 승부
Profile.sort(reverse = True)

Heavy = 0
cnt = 0
for x,y in Profile :
    if y >= Heavy :
        cnt +=1
        Heavy = y

print(cnt)

<창고 정리>
import sys
sys.stdin=open("input.txt", "r")

L= int(input())
storage = list(map(int,input().split()))
M = int(input())

storage.sort()

for i in range(M) :
    storage[0] +=1
    storage[L-1] -=1
    storage.sort()

print(storage[L-1] - storage[0])


<침몰하는 타이타닉>
import sys
sys.stdin=open("input.txt", "r")


N,M = map(int,input().split())

Weight = list(map(int,input().split()))

Weight.sort()

cnt = 0

while (Weight) :

    if len(Weight) == 1 :
        cnt +=1
        break
    # 2명 이하로 탈 수 있다 제한.
    # 제일 무거운 사람과 제일 가벼운 사람이 같이 탔을 때, 초과하는가?
    if Weight[0] + Weight[-1] > M :
        cnt += 1
        Weight.pop()
        
    else :
        Weight.pop(0)
        Weight.pop()
        cnt+=1


print(cnt)

# 만약 인원이 2명 제한이 풀린다면? -> 오류 발견(재검토)
import sys
sys.stdin=open("input.txt", "r")


N,M = map(int,input().split())

Weight = list(map(int,input().split()))

Weight.sort()

cnt = 0
weight = Weight[0]
length = 1

while (Weight) :

    if len(Weight) == 1 :
        cnt +=1
        break

    if weight > M :
        cnt += 1
        
        for i in range(1,length-1) :
            gap = len(Weight) - i
            Weight.pop(gap)
        Weight.pop(0)
        weight = Weight[0]
        length = 1
        
    else :
        weight += Weight[len(Weight) - length]
        length += 1


print(cnt)

<증가 수열>
# 값 자체로 접근
import sys
sys.stdin=open("input.txt", "r")

N = int(input())

num = list(map(int,input().split()))

last = 0
lt = num[0]
rt = num[len(num)-1]
res = ""


while True:

    if lt > last and rt > last:
        if lt < rt:
            res += 'L'
            last = lt
            num.pop(0)
            lt = num[0]
        else:
            res += 'R'
            last = rt
            num.pop()
            rt = num[len(num)-1]

    elif lt > last:
        res += 'L'
        last = lt
        num.pop(0)
        lt = num[0]


    elif rt > last:
        res += 'R'
        last = rt
        num.pop()
        rt = num[len(num)-1]

    else :
        break
    
print(len(res))
print(res)


# 투 포인터로 접근
import sys
sys.stdin=open("input.txt", "r")

N = int(input())

num = list(map(int,input().split()))

last = 0
lt = 0
rt = N-1
res = ""


while lt<=rt:
    if num[lt] > last and num[rt] > last:
        if num[lt] < num[rt]:
            res += "L"
            last = num[lt]
            lt += 1
        else:
            res += "R"
            last = num[rt]
            rt -= 1
            
    elif num[lt] > last:
        res += "L"
        last = num[lt]
        lt += 1
    elif num[rt] > last:
        res += "R"
        last = num[rt]
        rt -= 1

    else :
        break


    


# 4
# LRLL

print(len(res))
print(res)

<역수열>
import sys
sys.stdin=open("input.txt", "r")

N = int(input())
gap = list(map(int,input().split()))
# N개의 0이 입력된 리스트
num = [0] * N

for i in range(N) :
    
    for j in range(N) :
        if gap[i] == 0 and num[j] == 0 :
            num[j] =i+ 1
            break

        elif  num[j] == 0 :
            gap[i] -=1


for x in num :
    print(x, end=' ')
